# TechLink Web App

A full-stack e-commerce web app with a React + Vite frontend and a Node.js/Express backend.

## Run locally

- `cd client && npm install && npm run start`
- `cd server && npm install && npm start`

## What Changed

- Add About Us section to home.
- Fix frontend-backend connection by configuring routes. (should configure cors later on)
- Display 'top brand products' under 'best selling' section
